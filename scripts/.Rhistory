comment.char = ""
)
species_id_to_common <- setNames(tree_clade_map$tree_name, tree_clade_map$species_id)
new_names <- unname(species_id_to_common[names(seqs)])
names(seqs) <- ifelse(is.na(new_names) | new_names == "", names(seqs), new_names)
# Convert DNA to RNA for plotting
seqs <- gsub("T", "U", seqs, fixed = TRUE)
# Remove columns that are gaps/missing across all species
seqs <- drop_all_gap_columns(seqs)
# Save processed FASTA for manuscript / reuse
write_fasta_simple(seqs, fasta_processed)
seq_data <- readLines(fasta_processed)
seq_count <- sum(grepl("^>", seq_data))
p1 <- ggmsa(
fasta_processed,
char_width = 0.6,
seq_name = TRUE,
consensus_views = TRUE,
color = "Chemistry_NT"
) +
annotate(
"rect",
xmin = 27 - 0.5, xmax = 33 + 0.5,
ymin = -Inf, ymax = Inf,
alpha = 0.2, fill = "yellow", color = "orange", linewidth = 0.8
) +
annotate(
"text",
x = 30, y = seq_count + 0.5, label = "27-33",
color = "darkorange", fontface = "bold", size = 3.5
)
print(p1)
View(tree_clade_map)
species_id_to_common <- setNames(tree_clade_map$matched_common_name, tree_clade_map$species_id)
knitr::opts_chunk$set(
echo = TRUE,
warning = FALSE,
message = FALSE,
fig.width = 14,
fig.height = 12
)
library(ggplot2)
library(ggmsa)
# Load the hnRNPH1 decoy fasta produced in the heading
fasta_tracked <- file.path("hnRNPH1_intron4decoyMSA.fa")
tree_clade_mapping_file <- file.path("tree_to_clade_mapping.tsv")
fasta_processed <- file.path("hnRNPH1_intron4decoyMSA.processed.fa")
p0 <- ggmsa(
fasta_tracked,
char_width = 0.6,
seq_name = TRUE,
consensus_views = TRUE,
color = "Chemistry_NT"
)
print(p0)
read_fasta_simple <- function(path) {
x <- readLines(path)
header_idx <- which(startsWith(x, ">"))
seq_starts <- header_idx + 1
seq_ends <- c(header_idx[-1] - 1, length(x))
# FASTA headers in this project are like: "> hg38" (note the space).
# Normalize by removing ">" + any whitespace, then trimming.
headers <- trimws(sub("^>\\s*", "", x[header_idx]))
seqs <- vapply(
seq_along(headers),
function(i) paste0(x[seq_starts[i]:seq_ends[i]], collapse = ""),
character(1)
)
names(seqs) <- headers
seqs
}
write_fasta_simple <- function(seqs, path) {
out <- unlist(Map(function(h, s) c(paste0(">", h), s), names(seqs), unname(seqs)))
writeLines(out, path)
}
drop_all_gap_columns <- function(seqs) {
m <- do.call(rbind, strsplit(seqs, "", fixed = TRUE))
drop <- apply(m, 2, function(col) all(col %in% c("-", "*")))
m2 <- m[, !drop, drop = FALSE]
apply(m2, 1, paste0, collapse = "")
}
# Load alignment
seqs <- read_fasta_simple(fasta_tracked)
names(seqs) <- trimws(names(seqs))
# Remove species with large insertions and poor conservation in the region of interest
remove_species <- c(
"colLiv1", "falChe1", "falPer1", "ficAlb2", "zonAlb1", "geoFor1",
"taeGut2", "melUnd1", "araMac1", "anaPla1", "galGal4", "melGal1",
"allMis1", "cheMyd1", "chrPic2", "pelSin1", "anoCar2", "latCha1",
"pseHum1", "apaSpi1", "oryLat2"
)
seqs <- seqs[!(names(seqs) %in% remove_species)]
# Rename FASTA headers to common names using the mapping file
tree_clade_map <- read.table(
tree_clade_mapping_file,
header = TRUE,
sep = "\t",
stringsAsFactors = FALSE,
quote = "",
comment.char = ""
)
species_id_to_common <- setNames(tree_clade_map$matched_common_name, tree_clade_map$species_id)
new_names <- unname(species_id_to_common[names(seqs)])
names(seqs) <- ifelse(is.na(new_names) | new_names == "", names(seqs), new_names)
# Convert DNA to RNA for plotting
seqs <- gsub("T", "U", seqs, fixed = TRUE)
# Remove columns that are gaps/missing across all species
seqs <- drop_all_gap_columns(seqs)
# Save processed FASTA for manuscript / reuse
write_fasta_simple(seqs, fasta_processed)
seq_data <- readLines(fasta_processed)
seq_count <- sum(grepl("^>", seq_data))
p1 <- ggmsa(
fasta_processed,
char_width = 0.6,
seq_name = TRUE,
consensus_views = TRUE,
color = "Chemistry_NT"
) +
annotate(
"rect",
xmin = 27 - 0.5, xmax = 33 + 0.5,
ymin = -Inf, ymax = Inf,
alpha = 0.2, fill = "yellow", color = "orange", linewidth = 0.8
) +
annotate(
"text",
x = 30, y = seq_count + 0.5, label = "27-33",
color = "darkorange", fontface = "bold", size = 3.5
)
print(p1)
seq_data <- readLines(fasta_processed)
seq_count <- sum(grepl("^>", seq_data))
p1 <- ggmsa(
fasta_processed,
char_width = 0.6,
seq_name = TRUE,
consensus_views = TRUE,
color = "Chemistry_NT"
) +
annotate(
"rect",
xmin = 27 - 0.5, xmax = 33 + 0.5,
ymin = -Inf, ymax = Inf,
alpha = 0.2, fill = "yellow", color = "orange", linewidth = 0.8
) +
annotate(
"text",
x = 30, y = seq_count, label = "179,620,576-179,620,582",
color = "darkorange", fontface = "bold", size = 3.5
)
print(p1)
seq_data <- readLines(fasta_processed)
seq_count <- sum(grepl("^>", seq_data))
p1 <- ggmsa(
fasta_processed,
char_width = 0.6,
seq_name = TRUE,
consensus_views = TRUE,
color = "Chemistry_NT"
) +
annotate(
"rect",
xmin = 27 - 0.5, xmax = 33 + 0.5,
ymin = -Inf, ymax = Inf,
alpha = 0.2, fill = "yellow", color = "orange", linewidth = 0.8
) +
annotate(
"text",
x = 30, y = seq_count, label = "179,620,576-179,620,582",
color = "salmon", fontface = "bold", size = 3.5
)
print(p1)
seq_data <- readLines(fasta_processed)
seq_count <- sum(grepl("^>", seq_data))
p1 <- ggmsa(
fasta_processed,
char_width = 0.6,
seq_name = TRUE,
consensus_views = TRUE,
color = "Chemistry_NT"
) +
annotate(
"rect",
xmin = 27 - 0.5, xmax = 33 + 0.5,
ymin = -Inf, ymax = Inf,
alpha = 0.2, fill = "yellow", color = "orange", linewidth = 0.8
) +
annotate(
"text",
x = 30, y = seq_count + 2, label = "179,620,576-179,620,582",
color = "salmon", fontface = "bold", size = 4
)
print(p1)
knitr::opts_chunk$set(
echo = TRUE,
warning = FALSE,
message = FALSE,
fig.width = 14,
fig.height = 12
)
library(ggplot2)
library(ggmsa)
# Load the hnRNPH1 decoy fasta produced in the heading
fasta <- file.path("hnRNPH1_intron4decoyMSA.fa")
tree_clade_mapping_file <- file.path("tree_to_clade_mapping.tsv")
fasta_processed <- file.path("hnRNPH1_intron4decoyMSA.processed.fa")
p0 <- ggmsa(
fasta,
char_width = 0.6,
seq_name = TRUE,
consensus_views = TRUE,
color = "Chemistry_NT"
)
print(p0)
read_fasta_simple <- function(path) {
x <- readLines(path)
header_idx <- which(startsWith(x, ">"))
seq_starts <- header_idx + 1
seq_ends <- c(header_idx[-1] - 1, length(x))
# Normalize by removing ">" + any whitespace and trim
headers <- trimws(sub("^>\\s*", "", x[header_idx]))
seqs <- vapply(
seq_along(headers),
function(i) paste0(x[seq_starts[i]:seq_ends[i]], collapse = ""),
character(1)
)
names(seqs) <- headers
seqs
}
write_fasta_simple <- function(seqs, path) {
out <- unlist(Map(function(h, s) c(paste0(">", h), s), names(seqs), unname(seqs)))
writeLines(out, path)
}
drop_all_gap_columns <- function(seqs) {
m <- do.call(rbind, strsplit(seqs, "", fixed = TRUE))
drop <- apply(m, 2, function(col) all(col %in% c("-", "*")))
m2 <- m[, !drop, drop = FALSE]
apply(m2, 1, paste0, collapse = "")
}
# Load alignment
seqs <- read_fasta_simple(fasta)
names(seqs) <- trimws(names(seqs))
# Remove species with large insertions and poor conservation in the region of interest
remove_species <- c(
"colLiv1", "falChe1", "falPer1", "ficAlb2", "zonAlb1", "geoFor1",
"taeGut2", "melUnd1", "araMac1", "anaPla1", "galGal4", "melGal1",
"allMis1", "cheMyd1", "chrPic2", "pelSin1", "anoCar2", "latCha1",
"pseHum1", "apaSpi1", "oryLat2"
)
seqs <- seqs[!(names(seqs) %in% remove_species)]
# Rename FASTA headers to common names using the mapping file
tree_clade_map <- read.table(
tree_clade_mapping_file,
header = TRUE,
sep = "\t",
stringsAsFactors = FALSE,
quote = "",
comment.char = ""
)
species_id_to_common <- setNames(tree_clade_map$matched_common_name, tree_clade_map$species_id)
new_names <- unname(species_id_to_common[names(seqs)])
names(seqs) <- ifelse(is.na(new_names) | new_names == "", names(seqs), new_names)
# Convert DNA to RNA for plotting
seqs <- gsub("T", "U", seqs, fixed = TRUE)
# Remove columns that are gaps/missing across all species
seqs <- drop_all_gap_columns(seqs)
# Optionally save processed FASTA for reuse
write_fasta_simple(seqs, fasta_processed)
seq_data <- readLines(seqs)
# Load alignment
seqs <- read_fasta_simple(fasta)
names(seqs) <- trimws(names(seqs))
# Remove species with large insertions and poor conservation in the region of interest
remove_species <- c(
"colLiv1", "falChe1", "falPer1", "ficAlb2", "zonAlb1", "geoFor1",
"taeGut2", "melUnd1", "araMac1", "anaPla1", "galGal4", "melGal1",
"allMis1", "cheMyd1", "chrPic2", "pelSin1", "anoCar2", "latCha1",
"pseHum1", "apaSpi1", "oryLat2"
)
seqs <- seqs[!(names(seqs) %in% remove_species)]
# Rename FASTA headers to common names using the mapping file
tree_clade_map <- read.table(
tree_clade_mapping_file,
header = TRUE,
sep = "\t",
stringsAsFactors = FALSE,
quote = "",
comment.char = ""
)
species_id_to_common <- setNames(tree_clade_map$matched_common_name, tree_clade_map$species_id)
new_names <- unname(species_id_to_common[names(seqs)])
names(seqs) <- ifelse(is.na(new_names) | new_names == "", names(seqs), new_names)
# Convert DNA to RNA for plotting
seqs <- gsub("T", "U", seqs, fixed = TRUE)
# Remove columns that are gaps/missing across all species
seqs <- drop_all_gap_columns(seqs)
# Save processed FASTA for reuse
write_fasta_simple(seqs, fasta_processed)
seq_data <- readLines(fasta_processed)
seq_count <- sum(grepl("^>", seq_data))
p1 <- ggmsa(
fasta_processed,
char_width = 0.6,
seq_name = TRUE,
consensus_views = TRUE,
color = "Chemistry_NT"
) +
annotate(
"rect",
xmin = 27 - 0.5, xmax = 33 + 0.5,
ymin = -Inf, ymax = Inf,
alpha = 0.2, fill = "yellow", color = "orange", linewidth = 0.8
) +
annotate(
"text",
x = 30, y = seq_count, label = "179,620,576-179,620,582",
color = "black", fontface = "bold", size = 4
)
print(p1)
knitr::opts_chunk$set(
echo = TRUE,
warning = FALSE,
message = FALSE,
fig.width = 14,
fig.height = 12
)
library(ggplot2)
library(ggmsa)
# Load the hnRNPH1 decoy fasta produced in the heading
fasta <- file.path("hnRNPH1_intron4decoyMSA.fa")
tree_clade_mapping_file <- file.path("tree_to_clade_mapping.tsv")
fasta_processed <- file.path("hnRNPH1_intron4decoyMSA.processed.fa")
p0 <- ggmsa(
fasta,
char_width = 0.6,
seq_name = TRUE,
consensus_views = TRUE,
color = "Chemistry_NT"
)
print(p0)
read_fasta_simple <- function(path) {
x <- readLines(path)
header_idx <- which(startsWith(x, ">"))
seq_starts <- header_idx + 1
seq_ends <- c(header_idx[-1] - 1, length(x))
# Normalize by removing ">" + any whitespace and trim
headers <- trimws(sub("^>\\s*", "", x[header_idx]))
seqs <- vapply(
seq_along(headers),
function(i) paste0(x[seq_starts[i]:seq_ends[i]], collapse = ""),
character(1)
)
names(seqs) <- headers
seqs
}
write_fasta_simple <- function(seqs, path) {
out <- unlist(Map(function(h, s) c(paste0(">", h), s), names(seqs), unname(seqs)))
writeLines(out, path)
}
drop_all_gap_columns <- function(seqs) {
m <- do.call(rbind, strsplit(seqs, "", fixed = TRUE))
drop <- apply(m, 2, function(col) all(col %in% c("-", "*")))
m2 <- m[, !drop, drop = FALSE]
apply(m2, 1, paste0, collapse = "")
}
# Load alignment
seqs <- read_fasta_simple(fasta)
names(seqs) <- trimws(names(seqs))
# Remove species with large insertions and poor conservation in the region of interest
remove_species <- c(
"colLiv1", "falChe1", "falPer1", "ficAlb2", "zonAlb1", "geoFor1",
"taeGut2", "melUnd1", "araMac1", "anaPla1", "galGal4", "melGal1",
"allMis1", "cheMyd1", "chrPic2", "pelSin1", "anoCar2", "latCha1",
"pseHum1", "apaSpi1", "oryLat2"
)
seqs <- seqs[!(names(seqs) %in% remove_species)]
# Rename FASTA headers to common names using the mapping file
tree_clade_map <- read.table(
tree_clade_mapping_file,
header = TRUE,
sep = "\t",
stringsAsFactors = FALSE,
quote = "",
comment.char = ""
)
species_id_to_common <- setNames(tree_clade_map$matched_common_name, tree_clade_map$species_id)
new_names <- unname(species_id_to_common[names(seqs)])
names(seqs) <- ifelse(is.na(new_names) | new_names == "", names(seqs), new_names)
# Convert DNA to RNA for plotting
seqs <- gsub("T", "U", seqs, fixed = TRUE)
# Remove columns that are gaps/missing across all species
seqs <- drop_all_gap_columns(seqs)
# Save processed FASTA for reuse
write_fasta_simple(seqs, fasta_processed)
seq_data <- readLines(fasta_processed)
seq_count <- sum(grepl("^>", seq_data))
p1 <- ggmsa(
fasta_processed,
char_width = 0.6,
seq_name = TRUE,
consensus_views = TRUE,
color = "Chemistry_NT"
) +
annotate(
"rect",
xmin = 27 - 0.5, xmax = 33 + 0.5,
ymin = -Inf, ymax = Inf,
alpha = 0.2, fill = "yellow", color = "orange", linewidth = 0.8
) +
annotate(
"text",
x = 30, y = seq_count, label = "179,620,576-179,620,582",
color = "black", fontface = "bold", size = 4
)
print(p1)
knitr::opts_chunk$set(
echo = TRUE,
warning = FALSE,
message = FALSE,
fig.width = 14,
fig.height = 12
)
library(ggplot2)
library(ggmsa)
# Load the hnRNPH1 decoy fasta produced in the heading
fasta <- file.path("hnRNPH1_intron4decoyMSA.fa")
tree_clade_mapping_file <- file.path("tree_to_clade_mapping.tsv")
fasta_processed <- file.path("hnRNPH1_intron4decoyMSA.processed.fa")
p0 <- ggmsa(
fasta,
char_width = 0.6,
seq_name = TRUE,
consensus_views = TRUE,
color = "Chemistry_NT"
)
print(p0)
read_fasta_simple <- function(path) {
x <- readLines(path)
header_idx <- which(startsWith(x, ">"))
seq_starts <- header_idx + 1
seq_ends <- c(header_idx[-1] - 1, length(x))
# Normalize by removing ">" + any whitespace and trim
headers <- trimws(sub("^>\\s*", "", x[header_idx]))
seqs <- vapply(
seq_along(headers),
function(i) paste0(x[seq_starts[i]:seq_ends[i]], collapse = ""),
character(1)
)
names(seqs) <- headers
seqs
}
write_fasta_simple <- function(seqs, path) {
out <- unlist(Map(function(h, s) c(paste0(">", h), s), names(seqs), unname(seqs)))
writeLines(out, path)
}
drop_all_gap_columns <- function(seqs) {
m <- do.call(rbind, strsplit(seqs, "", fixed = TRUE))
drop <- apply(m, 2, function(col) all(col %in% c("-", "*")))
m2 <- m[, !drop, drop = FALSE]
apply(m2, 1, paste0, collapse = "")
}
# Load alignment
seqs <- read_fasta_simple(fasta)
names(seqs) <- trimws(names(seqs))
# Remove species with large insertions and poor conservation in the region of interest
remove_species <- c(
"colLiv1", "falChe1", "falPer1", "ficAlb2", "zonAlb1", "geoFor1",
"taeGut2", "melUnd1", "araMac1", "anaPla1", "galGal4", "melGal1",
"allMis1", "cheMyd1", "chrPic2", "pelSin1", "anoCar2", "latCha1",
"pseHum1", "apaSpi1", "oryLat2"
)
seqs <- seqs[!(names(seqs) %in% remove_species)]
# Rename FASTA headers to common names using the mapping file
tree_clade_map <- read.table(
tree_clade_mapping_file,
header = TRUE,
sep = "\t",
stringsAsFactors = FALSE,
quote = "",
comment.char = ""
)
species_id_to_common <- setNames(tree_clade_map$matched_common_name, tree_clade_map$species_id)
new_names <- unname(species_id_to_common[names(seqs)])
names(seqs) <- ifelse(is.na(new_names) | new_names == "", names(seqs), new_names)
# Convert DNA to RNA for plotting
seqs <- gsub("T", "U", seqs, fixed = TRUE)
# Remove columns that are gaps/missing across all species
seqs <- drop_all_gap_columns(seqs)
# Save processed FASTA for reuse
write_fasta_simple(seqs, fasta_processed)
seq_data <- readLines(fasta_processed)
seq_count <- sum(grepl("^>", seq_data))
p1 <- ggmsa(
fasta_processed,
char_width = 0.6,
seq_name = TRUE,
consensus_views = TRUE,
color = "Chemistry_NT"
) +
annotate(
"rect",
xmin = 27 - 0.5, xmax = 33 + 0.5,
ymin = -Inf, ymax = Inf,
alpha = 0.2, fill = "yellow", color = "orange", linewidth = 0.8
) +
annotate(
"text",
x = 30, y = seq_count, label = "179,620,576-179,620,582",
color = "black", fontface = "bold", size = 4
)
print(p1)
